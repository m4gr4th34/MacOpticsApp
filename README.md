# Simple Ray Tracing modeling project

Using AI and vibe coding, seeing how far one can get in creating an open-source, license-free, optical design software.<br>
You need to create a venv based on the requirements.txt <br>
then you'll need to run this command in CLI to generate the app in the /dist folder:  ./venv/bin/pyinstaller -y gui.spec
